{
  "name": "game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild src/game.js --bundle --format=iife --outfile=game.bundle.js --watch --sourcemap=inline",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.15.5",
    "kontra": "^8.0.0"
  }
}
